import {MyCreep} from "creep";
import {MyFlag} from "flag";
export class MyClaimer extends MyCreep {
    constructor (creep:Creep, claimFlags:MyFlag[]) {
        super(creep);
    }

    public runRoutine() {

    }
}